<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.CatalogoDeFilmes" parent="Theme.Material3.Dark.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="colorControlNormal">@color/colorDetail</item>
    </style>

    <style name="Theme.CatalogoDeFilmes" parent="Base.Theme.CatalogoDeFilmes" />

    <style name="Theme.Dialog.Loading" parent="Theme.Material3.Light.Dialog.Alert">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:layout_gravity">center</item>
    </style>
</resources>